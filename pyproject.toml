[project]
name = "code-generator"
version = "0.1.0"
description = "Python multi‑agent system using ADK, FastMCP and Ollama"
readme = "README.md"
requires-python = ">=3.13"

dependencies = []

[build-system]
requires = ["setuptools>=64", "wheel", "uv"]
build-backend = "setuptools.build_meta"
